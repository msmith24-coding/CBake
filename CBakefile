$CC = "g++";
$COMPILER_FLAGS = "--std=c++20";

$OBJS = "./src/*.cpp";
$OUT = "./bin/cbake";

action build:
    simple_compile($CC, $OBJS, $COMPILER_FLAGS, $OUT);
    print("Finished build action.");
end