$COMPILER = "g++";
$FLAGS = "--std=c++20";

$FILES = "./src/*.cpp";
$OUT = "./bin/cbake";

action clean:
    print("Cleaning all object files.");
    del("*.o");
end

action build:
    compile($COMPILER, $FLAGS, $FILES, $OUT);
    call("clean");
    print("Finished build action.");
end