$COMPILER = "g++";
$FLAGS = "--std=c++20 -c";

$FILES = "./src/*.cpp";
$OBJS = "*.o";
$OUT = "main";

action clean:
    del($OBJS);
end

action build:
    run($COMPILER + " " + $FLAGS + " " + $FILES);
    run($COMPILER + " " + $OBJS  + " -o " + $OUT);
    print("Finished.");
    call("clean");
end