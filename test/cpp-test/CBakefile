$COMPILER = "g++";
$FLAGS = "--std=c++20";

$FILES = "./src/*.cpp";
$OUT = "main";

action clean:
    del("*.o");
end

action build:
    compile($COMPILER, $FLAGS, $FILES, $OUT);
    print("Finished.");
    call("clean");
end