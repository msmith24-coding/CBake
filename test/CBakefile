$COMPILER = "g++";
$FLAGS = "--std=c++20 -c";

$FILES = "*.cpp";
$OBJS = "*.o";

action clean:
    run("del " + $OBJS);
end

action build:
    run($COMPILER + " " + $FLAGS + " " + $FILES);
    run($COMPILER + " " + $OBJS  + " -o main.exe");
    call("clean");
end