$CC : str = "g++";
$OUT : str = "main";

$FLAGS : list<str> = [
    "--std=c++20",
    "-Wall"
];

$OBJS : list<str> = [
    "main.cpp"
];

action build:
    print("Running build action...");
    simple_compile($CC, $OBJS, $FLAGS, $OUT);
    print("Finished build action.");
end